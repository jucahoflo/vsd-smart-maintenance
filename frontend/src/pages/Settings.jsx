import React from 'react';
import { Typography, Box } from '@mui/material';

const Settings = () => {
  return (
    <Box>
      <Typography variant="h4" fontWeight="bold" mb={3}>
        ⚙️ Configuración
      </Typography>
      <Typography>Módulo de Configuración - En construcción</Typography>
    </Box>
  );
};

export default Settings;
